"""Pure logic: no I/O, no HTTP, no filesystem. Mirrors the library's own
`domain/` layer, and for the same reason — these are the rules, and they must be
readable and testable without standing a server up.
"""
